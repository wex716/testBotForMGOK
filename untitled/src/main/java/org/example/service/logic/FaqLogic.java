package org.example.service.logic;

import org.example.statemachine.TransmittedData;
import org.telegram.telegrambots.meta.api.methods.send.SendMessage;

public class FaqLogic {

    public SendMessage processWaitingViewFAQ(String textFromUser, TransmittedData transmittedData) throws Exception {
        SendMessage messageToUser = new SendMessage();


        return messageToUser;
    }

    public SendMessage processWaitingViewProblemComputer(String textFromUser, TransmittedData transmittedData) throws Exception {
        SendMessage messageToUser = new SendMessage();


        return messageToUser;
    }

    public SendMessage processWaitingViewProblemPrinter(String textFromUser, TransmittedData transmittedData) throws Exception {
        SendMessage messageToUser = new SendMessage();


        return messageToUser;
    }

    public SendMessage processWaitingViewProblemProjector(String textFromUser, TransmittedData transmittedData) throws Exception {
        SendMessage messageToUser = new SendMessage();


        return messageToUser;
    }


}
