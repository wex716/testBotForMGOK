package org.example.service.logic;

import org.example.statemachine.TransmittedData;
import org.telegram.telegrambots.meta.api.methods.send.SendMessage;

public class HistoryApplicationLogic {
    public SendMessage processWaitingViewApplicationHistory(String textFromUser, TransmittedData transmittedData) throws Exception {
        SendMessage messageToUser = new SendMessage();


        return messageToUser;

    }

    public SendMessage processWaitingFirstViewCommands(String textFromUser, TransmittedData transmittedData) throws Exception {
        SendMessage messageToUser = new SendMessage();


        return messageToUser;

    }

    public SendMessage processWaitingMiddleViewCommands(String textFromUser, TransmittedData transmittedData) throws Exception {
        SendMessage messageToUser = new SendMessage();


        return messageToUser;

    }

    public SendMessage processWaitingLastViewCommands(String textFromUser, TransmittedData transmittedData) throws Exception {
        SendMessage messageToUser = new SendMessage();


        return messageToUser;

    }
}
